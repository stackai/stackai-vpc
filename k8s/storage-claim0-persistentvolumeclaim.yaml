apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: storage-claim0
  name: storage-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
