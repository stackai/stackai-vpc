apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: db-claim8
  name: db-claim8
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
