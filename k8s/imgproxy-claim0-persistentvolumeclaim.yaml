apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: imgproxy-claim0
  name: imgproxy-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
