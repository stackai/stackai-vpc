apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: caddy-config
  name: caddy-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
